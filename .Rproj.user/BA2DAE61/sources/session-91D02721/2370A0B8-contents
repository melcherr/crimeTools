# Init git in the /website/public/ folder
git init

# Add and commit the changes
git add .
git commit -m "Version 1.0.0"

# Set origin
git remote add origin https://github.com/melcherr/crimeTools

# Rename local branch
git branch -M main

# And push your updates online
git push -u origin main
